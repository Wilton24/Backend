<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Exercise 1</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <h3>Current second: <%= seconds %></h3>

    <% if(seconds % 2 == 0){ %>
    <ul>
      <% items.forEach((element) => { %>
      <li><%= element %></li>
      <% }); %>
    </ul>

    <% } else { %>
    <h2>Nothing to display here :s</h2>
    <% } %> <%- htmlContent %> <%- include('footer') %>
  </body>
</html>
